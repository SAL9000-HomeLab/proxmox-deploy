---
# roles/proxmox_clone/defaults/main.yml

# High-level defaults used by the role (non-sensitive)
proxmox_defaults:
  node: pve01.lab.sal9000.tech
  storage: nfs_ssd
  snippets_dir: /var/lib/vz/snippets
  disk_target: scsi0
  full_clone: false
  net_bridge: vnet30
  net_model: virtio
  vlan: 30
  timeout: 300
  timezone: "EST"

netbox:
  api_url: ""
  token: ""
  ssl_verify: true

netbox_allocate_ip: true
netbox_ip_ranges_by_bridge: {}
netbox_gateway_by_bridge: {}

# Default vm list (empty) - real VM definitions should be placed in group_vars or host_vars
provision_vms: []

# Example (commented) for maintainers:
# - name: web-01
#   template_vmid: 9000
#   vmid: null
#   node: pve01.lab.sal9000.tech
#   os_type: linux
#   description: "Internal web server for team X"
#   dns_name: "web-01.example.com"  # only applied when NetBox allocates the IP
#   cores: 2
#   memory: 2048
#   disk_size: 20G
#   ip: 192.168.100.101/24
#   gateway: 192.168.100.1
#   dns_servers:
#     - 1.1.1.1
#   search_domains:
#     - example.com
#   ssh_authorized_keys:
#     - "ssh-rsa AAAA... user@example"
#   cloudinit_userdata: {}  # optional additions to the jinja template context
