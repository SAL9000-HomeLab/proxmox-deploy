# Examples; omit ip/gw for DHCP
vms:
  - name: "w22-core-01"
    # node: "pve02"            # if set, placement logic will honor this and skip auto-placement
    vlan: 30
    ip: "10.100.30.121/24"
    gw: "10.100.30.1"
    memory: 8192
    cores: 4
    tags: "prod,win"
    template: "{{ template_name | default('W22StdCore') }}"

  - name: "w22-gui-01"
    vlan: 30
    # DHCP example
    tags: "test,win"
    template: "{{ template_name | default('W22StdGUI') }}"