all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    proxmox:
      hosts:
        pve01.lab.sal9000.tech:
          ansible_user: root
          ansible_ssh_private_key_file: /home/sermanre/.ssh/id_proxmox
          ansible_python_interpreter: /usr/bin/python3
          # optional: ansible_host: 10.0.0.10  # if you need to override DNS with IP
